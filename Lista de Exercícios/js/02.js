// Faça um Programa que peça um valor e mostre na tela se o valor é positivo ou negativo.
function E2() {
    const n = window.prompt("Digite um número número");
    if (n > 0) {
      Resposta2.value = ("O Número " + n + " é POSITIVO")
    } else {
      Resposta2.value = ("O Número " + n + " é NEGATIVO")
    }
  }